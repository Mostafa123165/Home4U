server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://149.102.135.15:3311/home4u?jdbcCompliantTruncation=false&allowMultiQueries=true
    username: home4u
    password: ${home4uPassword}

  cache:
    type: redis
    host: localhost
    port: 6379
    repositories:
      enabled: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true